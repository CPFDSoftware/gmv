#ifndef XmClientLibs
#define XmClientLibs $(XMLIB) $(XTOOLLIB) $(XLIB)
#endif

#include "../demos.cf"

EXTRA_INCLUDES = -I.

SRCS = bboxdemo.c 
OBJS = bboxdemo.o
LOCAL_LIBRARIES = -lXm -lXt -lXp -lX11 -lXext

ComplexProgramTarget(bboxdemo);


