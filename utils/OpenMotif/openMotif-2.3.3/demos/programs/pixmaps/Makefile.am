MAINTAINERCLEANFILES = Makefile.in

bindir = ${prefix}/share/Xm/pixmaps
datadir = ${prefix}/share/Xm/pixmaps

EXTRA_DIST  = crab.xpm eye.xpm porsche.xpm stopsign.xpm clown.xbm

data_DATA = $(EXTRA_DIST)
